using UnityEngine;

public class OverrideDamage : MonoBehaviour {
	/**
	 * Override the damage dealt to the target
	 *
	 * Works together with Hitpoints, which pulls the value from this component
	 * and uses it.
	 */
	public int Damage;
}
